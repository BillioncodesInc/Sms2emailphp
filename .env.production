# Production Environment Configuration for SE Gateway
# Copy this file and set these variables in Render.com Dashboard

# ==========================================
# Server Configuration
# ==========================================
NODE_ENV=production
PORT=10000
PHP_ENV=production

# ==========================================
# SMTP Configuration - NOW HANDLED VIA FRONTEND!
# ==========================================
# 🎉 NO SMTP ENVIRONMENT VARIABLES NEEDED!
#
# SMTP is now configured via the app's frontend:
# 1. Open your deployed app
# 2. Click "Config SMTP" button
# 3. Enter your Gmail/Outlook/etc credentials
# 4. Click "SET"
# 5. Config is saved to disk and persists across restarts!
#
# Benefits:
# - Configure once via UI, never again
# - Easy to change if SMTP gets blacklisted
# - Supports multiple SMTP accounts (bulk mode)
# - No need to redeploy when changing SMTP
#
# For more info, see: SMTP_PERSISTENCE.md

# ==========================================
# CORS Configuration
# ==========================================
# NOT NEEDED - Frontend and backend run on same instance!
# Apache proxy handles routing from frontend to backend
# ALLOWED_ORIGINS is no longer required

# ==========================================
# Backend URL Configuration
# ==========================================
# NOT NEEDED - Backend runs on localhost:9090
# Apache automatically proxies /api requests to Node.js backend
# No external backend URL configuration required

# ==========================================
# Security & Debug
# ==========================================
# Disable debug mode in production
DEBUG=false

# ==========================================
# Optional: Database Configuration
# ==========================================
# Uncomment and configure if you add database support later
# DATABASE_URL=postgresql://user:password@host:port/database
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=se_gateway
# DB_USER=admin
# DB_PASS=secure_password

# ==========================================
# Optional: Email Validation API
# ==========================================
# If using external email validation service
# EMAIL_VALIDATION_API_KEY=your-api-key-here

# ==========================================
# Optional: Rate Limiting
# ==========================================
# Requests per minute per IP
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# ==========================================
# Optional: Persistent Disk (Recommended for Production)
# ==========================================
# For 100% guaranteed SMTP config persistence, add a Render Persistent Disk:
#
# In render.yaml, uncomment the disk section:
#   disk:
#     name: smtp-storage
#     mountPath: /opt/render/project/src/backend/data
#     sizeGB: 1
#
# Cost: ~$0.25/GB/month = $0.25/month for 1GB
# Benefit: SMTP configs NEVER reset, even on major platform updates
#
# Without disk:
# - Free tier: Configs persist across most restarts (good for testing)
# - May occasionally reset on major Render platform updates

# ==========================================
# NOTES:
# ==========================================
# 1. NEVER commit this file with real values to GitHub
# 2. Set these as Environment Variables in Render Dashboard
# 3. Update ALLOWED_ORIGINS after deployment with your actual domain
# 4. SMTP is configured via app frontend (no env vars needed!)
# 5. For 100% guaranteed persistence, use Render Persistent Disk ($0.25/month)
